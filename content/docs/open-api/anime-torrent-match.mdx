---
title: 种子标题匹配番剧
description: 解析发布标题与种子内部名，返回 auto / manual 等决定。
full: true
_openapi:
  preload:
    - ./openapi/anibt.yaml
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          解析发布标题与种子内部名，返回 auto / manual 等决定。

          传入 `groupId` 时必须登录且属于该组。GET 返回 `405 METHOD_NOT_ALLOWED`。

          公开路由 `diagnostics=true` 返回 `403`。请求体上限 32 KiB。不缓存。

          Matcher 只在本接口。不要把调用方伪造的匹配结果传给发布接口。

          发布接口必须带 `animeIdType` 与 `animeId`。

          `limit` 默认 10，上限 50。速率限制默认 180 次 / 60 秒 / IP。

          成功响应直接返回 version、content、decision、candidates 等字段，没有 ok 或 data 包装。

          未提供 infoHashHex 时 decisionToken 和 decisionTokenExpiresAt 为 null。只有
          decision.kind=auto 时才可采用已列入 candidates 的决定；manual 必须由调用方确认。
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/anibt.yaml" operations={[{"path":"/api/animes/torrent-match","method":"post"}]} />
    </>
  );
}