---
title: 其他類 RSS
description: GET /rss/other/<category>.xml —— 漫畫、音樂、RAW、舞台劇各自獨立的 RSS，不混入主 RSS。
---

`/rss/other/<category>.xml` 輸出其他類資源的獨立 RSS。目前支援 `manga`、`music`、`raw`、`stage` 四個分類，對應漫畫、音樂、RAW、舞台劇。

<Callout type="warn">
  其他類資源**不會**混入 `/rss/magnets.xml`、`/rss/anime.xml` 或番劇詳情頁 RSS。需要訂閱這些資源時，請直接使用本頁的分類 RSS。
</Callout>

## URL

```txt title="分類 RSS"
https://anibt.net/rss/other/manga.xml
https://anibt.net/rss/other/music.xml
https://anibt.net/rss/other/raw.xml
https://anibt.net/rss/other/stage.xml
```

## 查詢參數

<TypeTable
  type={{
    'limit': {
      description: '返回條數，1..100，預設 100。只接受單個無前導零的十進位整數。',
      type: 'integer',
      default: '100',
    },
    'q': {
      description: '關鍵詞搜尋。別名：search。NFKC 規範化、去首尾空白、折疊連續空白，UTF-8 最多 160 bytes。',
      type: 'string',
    },
    'search': {
      description: 'q 的別名；q/search 合計只能出現一個值。',
      type: 'string',
    },
    'groupId': {
      description: '字幕組組織 ID。釋出 API 返回的 groupId 可直接用於過濾。',
      type: 'string',
    },
    'sortField': {
      description: 'publishedAt / fileSize，預設 publishedAt。',
      type: 'enum',
      default: 'publishedAt',
    },
    'sortOrder': {
      description: 'desc / asc，預設 desc。',
      type: 'enum',
      default: 'desc',
    },
  }}
/>

## 示例

<Steps>
  <Step>
    ### 訂閱漫畫最新釋出
    ```txt title="漫畫"
    https://anibt.net/rss/other/manga.xml
    ```
  </Step>
  <Step>
    ### 搜尋音樂分類
    ```txt title="音樂 + 關鍵詞"
    https://anibt.net/rss/other/music.xml?q=FLAC&limit=50
    ```
  </Step>
  <Step>
    ### 按檔案大小排序 RAW
    ```txt title="RAW + 檔案大小"
    https://anibt.net/rss/other/raw.xml?sortField=fileSize&sortOrder=desc
    ```
  </Step>
</Steps>

## 回應

返回標準 RSS 2.0 原生 XML。瀏覽器直開 RSS URL 時不載入站點自訂 XSL/CSS，會使用瀏覽器預設 XML 樹展示；RSS 閱讀器和 BT 客戶端繼續按標準欄位消費。

每個 `<item>` 包含：

<TypeTable
  type={{
    '<title>': {
      description: '資源標題；如果有字幕組名，會顯示為 [字幕組] 標題。',
      type: 'string',
    },
    '<link>': {
      description: '其他類資源詳情頁（/other-release/<id>）。',
      type: 'url',
    },
    '<guid isPermaLink="false">': {
      description: '穩定 other release id。',
      type: 'string',
    },
    '<pubDate>': {
      description: 'RFC 822 時間。',
      type: 'datetime',
    },
    '<description>': {
      description: 'CDATA，包含分類、字幕組、檔案大小、下載連結、說明和標籤。',
      type: 'string',
    },
    '<enclosure>': {
      description: '種子下載 URL，type="application/x-bittorrent"。僅上傳過種子檔案的條目會有。',
      type: 'url',
    },
    '<torrent>': {
      description: 'RSS 內的 torrent 元資訊；有 magnet 時會包含 infoHash。',
      type: 'element',
    },
  }}
/>

## 快取與索引

- RSS 回應支援 `ETag` / `If-None-Match`，未變更時返回 `304 Not Modified`。
- 無 `q` 的 latest/filter 變體為 `s-maxage=120`；帶 `q` 的搜尋（包括空結果）為 `s-maxage=600`；兩類統一 `stale-while-revalidate=60`。
- 搜尋快取身份只保存規範化查詢的 SHA-256。重複/空/控制字元 q、同時傳 q+search 或超過 160 bytes 返回 `400 + no-store`；規範化等價 URL 返回可快取一天的 `308`。
- 只有真正穿透 CDN/Redis 的新搜尋 MISS 才受 distinct-query 與全站併發預算保護；超限返回 `429 + Retry-After`，穩定查詢和快取 HIT 不扣額度。
- 這些 RSS 回應會帶 `noindex, follow` 搜尋引擎策略，避免分類 feed 被搜尋引擎索引。

繼續：[釋出其他類資源](/docs/open-api/other-releases-publish)；番劇主 RSS 見 [RSS 主入口](/docs/open-api/rss-anime)。
