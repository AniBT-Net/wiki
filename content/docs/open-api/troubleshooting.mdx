---
title: 调试与常见问题
description: 一行 curl 调试 + RSS 接入常见问题解答。
---

## 调试快查

<Tabs items={['看 RSS 内容', '验证 304', '查全部 slug', '查目标字幕组']}>
  <Tab value="看 RSS 内容">
    ```bash title="前 3 个 item"
    curl -s 'https://anibt.net/rss/anime.xml?bgmId=543360&groupSlug=kirara-fantasia&limit=3' \
      | grep -E '<title>|<pubDate>'
    ```
  </Tab>
  <Tab value="验证 304">
    ```bash title="ETag 复用"
    H=$(curl -sI 'https://anibt.net/rss/anime.xml?bgmId=543360')
    echo "$H" | grep -iE 'etag|last-modified'
    ETAG=$(echo "$H" | grep -i '^etag:' | cut -d' ' -f2- | tr -d '\r')
    curl -sI -H "If-None-Match: $ETAG" 'https://anibt.net/rss/anime.xml?bgmId=543360' | head -1
    ```
  </Tab>
  <Tab value="查全部 slug">
    ```bash title="全部字幕组"
    curl -s https://anibt.net/api/subtitle-groups \
      | jq '.data[] | {name, slug, totalReleases}'
    ```
  </Tab>
  <Tab value="查目标字幕组">
    ```bash title="按名字过滤"
    curl -s https://anibt.net/api/subtitle-groups \
      | jq '.data[] | select(.name | test("Kirara Fantasia|Prejudice-Studio|北宇治字幕组|拨雪寻春")) | {name, slug}'
    ```
  </Tab>
</Tabs>

## 常见问题

<Accordions>
  <Accordion title="slug 和 organizationId 有什么区别？">
    `slug` 是公开 URL 用的稳定字符串；`organizationId` 是内部主键。**第三方一律用 `slug`。**
  </Accordion>
  <Accordion title="为什么我的 If-Modified-Since 没生效？">
    按 RFC 7232，**ETag（`If-None-Match`）优先**。如果你同时发了两个头，服务端只看 ETag。建议永远用 ETag。
  </Accordion>
  <Accordion title="为什么 RSS 返回 308、400、403 或 429？">
    `308` 表示 URL 可规范化（例如显式 `limit=100`、参数顺序/大小写/空白等），请跟随跳转并保存最终 URL。`400 + no-store` 表示参数违反公开合同：`limit` 必须是单个 `1..100` 整数；`q`/`search` 合计只能一个值，规范化后不能为空、不能含控制字符且最多 160 UTF-8 bytes。生产 Cloudflare 可能在应用前用 `403` 拦截非法 limit。`429` 只保护真正穿透缓存的新高基数搜索 MISS；按 `Retry-After` 退避即可。
  </Accordion>
  <Accordion title="episode=5 会匹配 episodeKey='05' 吗？">
    是的，二者都会过滤；但 `episode` 是数字精确匹配，`episodeKey` 是字符串原样匹配。合集请用 `episodeKey=BATCH`。
  </Accordion>
  <Accordion title="我想知道某个 release 的种子直链怎么办？">
    RSS 里 `<enclosure url>` 已经是可直接喂给 BT 客户端的磁力 URL（带默认 tracker），`<torrent>` 元素里也带 infoHash。
  </Accordion>
  <Accordion title="接口会变吗？">
    路径、字段和规范参数会保持稳定；但为保护公共服务，安全上限与非法输入处理可能收紧并在 changelog 公告。客户端应遵守当前文档、跟随 308、处理 400/403/429，而不是依赖静默截断。`slug` 一旦发布也尽量保持稳定。
  </Accordion>
  <Accordion title="为什么我配置了极严格的过滤后返回数量很少？">
    `/rss/anime.xml` 现在由 Web runtime + Typesense 生成，分辨率、语言、格式、字幕方式等 Tag 会尽量下推到 Typesense filter。极严格过滤时返回少通常说明真实匹配少；如果后过滤窗口不足以保证结果完整，服务端会返回 `503`，不会回退 Convex 扫库。详见 [限流与上限](/docs/open-api/limits)。
  </Accordion>
  <Accordion title="新发的种子多久能在 RSS 里看到？">
    发布接口的 `200 OK` 表示写入已接受，不是同步可见性确认。站点侧刷新由 Typesense 完成、不可变快照发布、Redis 严格预热和 Edge CDN purge 事件驱动，不需要等满 120 秒 TTL；运维上，publication pending 超过 15 秒就进入告警检查。你在 RSS 阅读器里实际看到的时间还要加上客户端自己的 5–10 分钟轮询间隔。详见 [HTTP 缓存与 304](/docs/open-api/caching)。
  </Accordion>
  <Accordion title="为什么 DELETE 返回 202，而不是立刻 200？">
    `202 Accepted` 表示删除已被可靠接受，但 Typesense、列表/RSS、Redis、Cloudflare 和 R2 的严格清理屏障仍在运行。保存 `operationId`，按 `Location` / `statusUrl` 请求 `GET /api/releases/{releaseId}/deletion`，并遵守 `Retry-After`。重复 DELETE 是幂等的，会返回同一回执；只有 `state=completed` 才表示 AniBT 自有公开面已经完成不可见性证明。详见 [删除番剧发布](/docs/open-api/releases-delete)。
  </Accordion>
  <Accordion title="API Key 明明有效，为什么删除返回 403？">
    删除和状态查询都要求 `releases:delete`。scope 上线前创建的旧 Key 被安全迁移为仅 `releases:publish`，不会自动获得删除权。调用 [`GET /api/subtitle-groups/me`](/docs/open-api/me) 检查 `scopes`，再创建或改用显式包含 `releases:delete` 的 Key。缺 scope 会在资源查询前返回 `403 INSUFFICIENT_SCOPE`；有 scope 但无权访问目标时则与不存在资源一样返回 404。
  </Accordion>
  <Accordion title="删除 completed 后，外站、Tracker 和本地缓存也会消失吗？">
    不会全部消失。`completed` 覆盖 AniBT 自有 canonical 记录、详情/种子读取、Typesense 搜索与列表、magnets、公开/订阅 RSS、Redis、Cloudflare CDN 和捕获的 R2 对象。它不召回已经打开或被浏览器/RSS 客户端缓存的内容、已经下载的 torrent/magnet，不修改 Tracker swarm/白名单，也不撤回 Nyaa、Mikan、DMHY 等外站帖子；这些是独立流程。
  </Accordion>
  <Accordion title="为什么状态码、generatedAt 或 cf-cache-status 都正常，正文却还是旧的？">
    这些字段只说明请求和某一层缓存成功，不能证明触发发布的资源已经进入响应。请直接在详情页及对应 RSS / magnets 正文中检查返回的 `releaseId`，并同时记录 ETag / Last-Modified。运维侧再对比 snapshot queue age、publication pending age 与 `snapshot_redis_warm.completed.latencyMs`：queue age 高但 warm latency 低，应该处理调度或 backlog，而不是加长 Redis TTL。不要等待下一次无关发布“顺便刷新”。
  </Accordion>
</Accordions>

## 接入 checklist

最后过一遍：

<Steps>
  <Step>
    ### 启动时拉一次 [`/api/subtitle-groups`](/docs/open-api/subtitle-groups)
    本地缓存 `name → slug` 映射，避免硬编码 slug。
  </Step>
  <Step>
    ### 用 [`/rss/anime.xml`](/docs/open-api/rss-anime) 或短链订阅
    优先 `bgmId + groupSlug` 组合；不需要过滤时用 `/rss/group/<slug>.xml`。
  </Step>
  <Step>
    ### 永远带 If-None-Match
    缓存上次响应的 `ETag`，下次请求带 `If-None-Match`，详见 [HTTP 缓存与 304](/docs/open-api/caching)。
  </Step>
  <Step>
    ### 跟随 308 并保存规范 URL
    不要反复请求会跳转的旧参数形式；使用客户端最终 URL 作为后续轮询键。
  </Step>
  <Step>
    ### 间隔 ≥ 5 分钟
    更短没收益，且容易触发 429。
  </Step>
  <Step>
    ### 见 429 用 Retry-After 退避
    不要立即重试。
  </Step>
</Steps>
