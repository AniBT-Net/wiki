---
title: "介面約定與鑑權"
description: "依端點辨識鑑權、回應包裝和錯誤正文。"
---

呼叫公開 API 前，先確認對應端點的鑑權方式和回應結構。

<Callout type="info">
  https://anibt.net · [OpenAPI 3.1](/openapi.yaml)
</Callout>

<TypeTable type={{
  "公開目錄與查詢": {
    "type": "匿名",
    "description": "字幕組目錄、Bangumi 搜尋、季度番劇、單番劇字幕組、公開 RSS 和種子下載不需要 API Key。"
  },
  "釋出、刪除、同步狀態與身份查詢": {
    "type": "字幕組 API Key",
    "description": "使用 Authorization: Bearer <KEY> 或 X-API-Key；同時提供時 Bearer 優先。"
  },
  "個人訂閱 RSS": {
    "type": "個人 RSS 權杖",
    "description": "使用帳戶產生的完整 URL，key 為憑證，token 為相容別名。不是字幕組 API Key。"
  },
  "標題匹配 groupId": {
    "type": "登入工作階段",
    "description": "不傳 groupId 可匿名匹配；傳入則需要登入且屬於該組，API Key 不能取代工作階段。"
  }
}} />

<TypeTable type={{
  "releases:publish": {
    "type": "權限",
    "description": "POST 番劇或其他類釋出，包括番劇 preview。GET whoami 與 /me 只要求有效 Key，不要求此 scope。"
  },
  "releases:delete": {
    "type": "權限",
    "description": "DELETE /api/releases/{releaseId} 及 GET 對應 /deletion。"
  },
  "releases:sync:read": {
    "type": "權限",
    "description": "查詢本組 rel_* 資源的六站同步狀態。"
  }
}} />

成功回應依端點解析：目錄、查詢、/me 和同步狀態使用 data；釋出、whoami、刪除使用 result；標題匹配直接回傳 version、decision、candidates。番劇預覽使用頂層 previewUrl。

應用程式 JSON 錯誤包含 ok: false 和 error.code、error.message、error.requestId；reason、retryable、retryAfterMs、details 為選填欄位。缺少 retryable 不表示可以重試。公開 RSS 錯誤通常是純文字；Torznab 協定錯誤是 XML，HTTP 仍可能為 200；邊緣攔截可能回傳 HTML。先檢查 HTTP 狀態與 Content-Type，再解析正文。

釋出前用 GET 同一路徑核驗 groupSlug 與 scopes。釋出回執只保證 releaseId、revision，可能包含 torrentObjectId，不包含字幕組身份或 fileSize。資源 ID 使用端點要求的 rel_* 或 orel_*；字幕組通常用 slug，但其他類 RSS 的 groupId 使用組織 ID。
