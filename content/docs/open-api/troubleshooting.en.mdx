---
title: "Troubleshooting"
description: "Diagnose endpoint shapes, status codes, and error bodies."
---

Check the URL, HTTP method, Content-Type, and response body first.

<Callout type="info">
  https://anibt.net · [OpenAPI 3.1](/openapi.yaml)
</Callout>

<TypeTable type={{
  "Missing data or result": {
    "type": "Response shape",
    "description": "Title matching has no envelope; publish uses result; sync status uses data.sites[]; previewUrl is top-level."
  },
  "JSON parsing fails": {
    "type": "Content type",
    "description": "Public RSS errors may be plain text, Torznab uses XML, and edge blocks may be HTML."
  },
  "401 / 403": {
    "type": "Authentication",
    "description": "401: check key validity. 403: check scope or session membership. whoami and /me do not require releases:publish."
  },
  "422 / TORRENT_REQUIRED": {
    "type": "Publish input",
    "description": "Upload torrent with multipart, or use this group’s stored torrentStorageId with JSON. Anime JSON also requires magnetBase64."
  },
  "422 / VALIDATION_ERROR": {
    "type": "Fields",
    "description": "anime and bgmId are not accepted aliases in anime publish JSON. Use animeIdType and animeId."
  },
  "308": {
    "type": "Canonical URL",
    "description": "Follow and save Location. Public RSS removes defaults, aliases, and unsupported parameters."
  },
  "DELETE returns 202": {
    "type": "Pending operation",
    "description": "The initial DELETE is pending. A successful GET /deletion returns 200; inspect result.state."
  },
  "Deletion failed": {
    "type": "Stop polling",
    "description": "DELETE can return 500 RELEASE_DELETION_FAILED with a receipt in error.details. Status reads can return 200 with result.state=failed."
  },
  "500 UPLOAD_FAILED on publish": {
    "type": "Failed submission",
    "description": "Do not assume no write occurred or immediately retry a batch. Application throttling may use this response. Check existing releases and keep the request ID."
  },
  "External copy remains after deletion": {
    "type": "Scope",
    "description": "Deleting from AniBT does not withdraw copies already published or copied to external sites."
  }
}} />

Application JSON errors identify the request in error.requestId, usually also in X-Request-ID. Plain text, XML, redirects, and edge responses do not guarantee this field. Report the endpoint, time, HTTP status, and available request ID; never include keys, cookies, or private RSS URLs.
