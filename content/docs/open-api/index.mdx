---
title: "开放 API 总览"
description: "AniBT 公开查询、RSS、种子下载与字幕组 API。"
---

AniBT 提供公开查询与订阅接口，以及通过字幕组 API Key 调用的发布、删除和同步状态接口。

<Callout type="info">
  https://anibt.net · [OpenAPI 3.1](/openapi.yaml)
</Callout>

<TypeTable type={{
  "公开查询": {
    "type": "匿名",
    "description": "字幕组目录、Bangumi 搜索、季度番剧、单番剧字幕组和标题匹配。"
  },
  "发布与管理": {
    "type": "字幕组 API Key",
    "description": "发布番剧和其他类资源，核验身份，删除本组番剧资源，查询六站同步状态。"
  },
  "订阅与下载": {
    "type": "RSS / .torrent",
    "description": "公开 RSS、个人 RSS、种子下载，以及 Sonarr / Radarr / Prowlarr 使用的 Torznab。"
  }
}} />

生产 Base URL 为 `https://anibt.net`。API Key、个人 RSS 令牌和登录会话适用于不同接口，不能互相替代。成功响应的包装、空值及错误格式按每个端点定义处理。

机器可读定义见 [/openapi.yaml](/openapi.yaml)。接口页展示请求字段和响应示例；标题匹配是公开查询，但传入 groupId 时要求登录会话及该组成员身份。

<Cards>
  <Card title="接口约定与鉴权" href="/docs/open-api/reference" />
  <Card title="限流与参数上限" href="/docs/open-api/limits" />
  <Card title="HTTP 缓存与条件请求" href="/docs/open-api/caching" />
  <Card title="排障与常见问题" href="/docs/open-api/troubleshooting" />
  <Card title="Torznab" href="/docs/open-api/torznab" />
</Cards>

## 番剧查询与评分

`/api/seasons/anime` 的 `scope.kind` 区分季度浏览、关键词搜索和精确 ID 查询。全库关键词搜索的 `requestedSeason` 为 `null`，每部作品使用自己的季度；首播日期与季度分类分开。`premiere.precision` 为 `day`、`month`、`season` 或 `year`，粗精度日期使用周期首日。

季度浏览最多返回 200 条，关键词搜索最多 50 条。`total` 是本次返回数，`truncated=true` 表示还有结果；完整翻页使用 `/api/animes/search`。`rating` 使用 0–10 分，缺有效来源评分时返回 `5.0`；`averageScore` 使用 0–100 分，对应默认 `50`。现有 ani-rss 可直接排序，但旧版本不读取截断提示，仍最多显示 50 条搜索结果。

默认评分会在后续同步取得有效来源评分时自动更新；人工锁定的评分保留。资料更新沿用既有同步频率，浏览页面或订阅 RSS 不会逐条刷新上游资料。

## 播出状态

自动判定完结需要明确的首播日期和结束日期，并且来源在结束日完整过去后再次确认结束日期。日期按日本时间比较；只有年份、月份、季度或作品类型，不能据此判定完结。

来源更正播出计划后，自动状态会随同步重新计算。人工锁定的状态以及明确的停播、取消状态优先。证据不足的作品标为未知，不沿用缺乏依据的历史状态，也不把计划结束日当成已完结证明。

首页同一作品同一天只显示最新一集；正常跨日连播仍分别显示。
