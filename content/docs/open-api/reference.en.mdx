---
title: "Conventions and authentication"
description: "Authentication, response envelopes, and error formats by endpoint."
---

Check each endpoint’s authentication and response shape before integrating.

<Callout type="info">
  https://anibt.net · [OpenAPI 3.1](/openapi.yaml)
</Callout>

<TypeTable type={{
  "Public queries": {
    "type": "Anonymous",
    "description": "Group directory, Bangumi search, seasonal anime, anime groups, public RSS, and torrent downloads do not require an API Key."
  },
  "Publishing, deletion, sync status and identity": {
    "type": "Group API Key",
    "description": "Use Authorization: Bearer <KEY> or X-API-Key. Bearer takes precedence."
  },
  "Personal RSS": {
    "type": "Personal RSS token",
    "description": "Use the full account-generated URL. key is the credential; token is an alias. This is not a group API Key."
  },
  "Title matching with groupId": {
    "type": "Signed-in session",
    "description": "Omit groupId for anonymous matching. Supplying it requires a session and membership in that group; an API Key does not replace the session."
  }
}} />

<TypeTable type={{
  "releases:publish": {
    "type": "Scope",
    "description": "POST anime or other releases, including anime preview. GET whoami and /me only require a valid key, without this scope."
  },
  "releases:delete": {
    "type": "Scope",
    "description": "DELETE /api/releases/{releaseId} and GET its /deletion status."
  },
  "releases:sync:read": {
    "type": "Scope",
    "description": "Read six-site sync status for rel_* releases belonging to the key’s group."
  }
}} />

Parse success responses per endpoint: catalog queries, /me, and sync status use `data`; publishing, whoami, and deletion use `result`; title matching directly returns `version`, `decision`, and `candidates`. Anime preview returns a top-level `previewUrl`.

Application JSON errors contain `ok: false` and `error.code`, `error.message`, and `error.requestId`. `reason`, `retryable`, `retryAfterMs`, and `details` are optional. A missing retryable flag does not authorize retries. Public RSS errors are usually plain text. Torznab protocol errors are XML and may have HTTP 200. Edge blocks may be HTML. Check status and Content-Type before parsing.

Before publishing, GET the same endpoint and check groupSlug and scopes. A publish receipt guarantees releaseId and revision, with an optional torrentObjectId; it does not include group identity or fileSize. Use rel_* or orel_* as required. Group filters usually take a slug; other-category RSS groupId takes an organization ID.
