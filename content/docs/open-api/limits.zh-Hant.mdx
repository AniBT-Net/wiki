---
title: "限流與參數上限"
description: "各端點的預設限額、參數範圍與退避方式。"
---

以下是應用程式預設值，實際限制可能受部署設定和邊緣規則影響。

<Callout type="info">
  https://anibt.net · [OpenAPI 3.1](/openapi.yaml)
</Callout>

<TypeTable type={{
  "公開 RSS": {
    "type": "120 次 / 60 秒 / IP",
    "description": "番劇、字幕組、磁力、其他類 RSS；limit 預設 100，上限 100，沒有分頁游標。"
  },
  "個人訂閱 RSS": {
    "type": "30 次 / 60 秒 / IP",
    "description": "limit 預設 50，上限 200；重複無效 key 另有限制。"
  },
  "Torznab": {
    "type": "60 次 / 60 秒 / IP",
    "description": "limit 預設 100，上限 100；最新列表 offset + limit 最多 300，關鍵字搜尋最多 100。"
  },
  "Bangumi 搜尋": {
    "type": "60 次 / 60 秒 / IP",
    "description": "limit 預設 5，1–25；q 去除首尾空白後最多 100 字元。"
  },
  "字幕組目錄": {
    "type": "60 次 / 60 秒 / IP",
    "description": "完整目錄，不支援 limit 或分頁。"
  },
  "季度番劇與單番劇字幕組": {
    "type": "共用 30 次 / 60 秒 / IP",
    "description": "季度標題 query 最多 120 字元；單番劇字幕組每組最多回傳 30 筆最近資源。"
  },
  "標題匹配": {
    "type": "180 次 / 60 秒 / IP",
    "description": "limit 預設 10，1–50；rawTitle 和 torrentName 最多 1000 字元；請求正文上限 32 KiB。"
  },
  "API 釋出": {
    "type": "每類 120 次 / 60 秒 / IP",
    "description": "JSON 預設上限 1 MiB，multipart 預設上限 8 MiB。目前應用程式限流回傳 500 UPLOAD_FAILED；GET whoami 不消耗此額度。"
  },
  "刪除與刪除狀態": {
    "type": "30 / 120 次 / 60 秒 / Key",
    "description": "DELETE 預設 30 次，GET /deletion 預設 120 次。"
  },
  "同步狀態": {
    "type": "120 次 / 60 秒 / Key",
    "description": "GET /sync-status。"
  }
}} />

公開 RSS 的 limit 只接受無前導零的整數 1..100，重複、空值或格式錯誤回傳 400；預設 limit=100 會正規化為省略參數的 URL。個人 RSS 使用獨立規則。

RSS 的 q/search 與 Torznab 的 q 正規化後最多 160 個 UTF-8 位元組。Bangumi 搜尋、季度標題查詢與標題匹配各有字元數限制，不能共用長度規則。公開 RSS 沒有 cursor/page 分頁；Torznab 使用有界 offset。

收到 429 時依 Retry-After 等待，單位為秒。沒有此欄位的錯誤不能假定 2 秒後成功。普通 RSS 建議至少間隔 5 分鐘並避免並行請求；304 仍可能消耗額度。
