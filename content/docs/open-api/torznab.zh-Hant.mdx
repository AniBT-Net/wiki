---
title: Torznab 介面
description: 在 Sonarr、Radarr、Prowlarr 中把 AniBT 新增為 Torznab 索引器，自動搜尋和抓取番劇資源。
---

AniBT 提供 Torznab 介面。Sonarr、Radarr 和 Prowlarr 可以直接把 AniBT 新增為索引器。

<Callout type="info">
  不需要註冊，也不需要 API Key。用戶端裡的 **API Key** 留空即可，介面不會讀取它。
</Callout>

<Callout title="GET /torznab/api">
  用戶端裡的 **URL** 填 `https://anibt.net/torznab`，**API Path** 保持預設的 `/api`。
</Callout>

## 新增索引器

<Tabs items={['Sonarr', 'Radarr', 'Prowlarr']}>
  <Tab value="Sonarr">
    <Steps>
      <Step>
        ### 新建索引器
        進入 **Settings** → **Indexers**，點選 **+**，選擇 **Torznab**。
      </Step>
      <Step>
        ### 填寫位址
        **URL** 填 `https://anibt.net/torznab`，**API Path** 保持 `/api`，**API Key** 留空。
      </Step>
      <Step>
        ### 選擇分類
        **Anime Categories** 選擇 `5070`（Anime）。**Categories** 可以清空。
      </Step>
      <Step>
        ### 測試並儲存
        點選 **Test**，通過後儲存。番劇的 **Series Type** 設為 **Anime**。
      </Step>
    </Steps>
  </Tab>

  <Tab value="Radarr">
    <Steps>
      <Step>
        ### 新建索引器
        進入 **Settings** → **Indexers**，點選 **+**，選擇 **Torznab**。
      </Step>
      <Step>
        ### 填寫位址
        **URL** 填 `https://anibt.net/torznab`，**API Path** 保持 `/api`，**API Key** 留空。
      </Step>
      <Step>
        ### 選擇分類
        **Categories** 選擇 `2000`（Movies）和 `2020`（Movies/Other）。Radarr 只會拿到劇場版資源。
      </Step>
    </Steps>
  </Tab>

  <Tab value="Prowlarr">
    <Steps>
      <Step>
        ### 新建索引器
        進入 **Indexers** → **Add Indexer**，搜尋並選擇 **Generic Torznab**。
      </Step>
      <Step>
        ### 填寫位址
        **URL** 填 `https://anibt.net/torznab`，**API Path** 保持 `/api`，**API Key** 留空。
      </Step>
      <Step>
        ### 同步到 Sonarr / Radarr
        Prowlarr 按分類同步：`5070` 同步給 Sonarr，`2000` 同步給 Radarr。
      </Step>
    </Steps>
    Prowlarr 和 Jackett 內建的「AniBT」定義讀取的是 RSS，做種數固定顯示為 1。建議改用 Generic Torznab。
  </Tab>
</Tabs>

## 回傳內容

<TypeTable
  type={{
    '資源範圍': {
      type: '番劇資源',
      description: '只包含帶種子檔案的番劇資源。漫畫、音樂、生肉等其他類資源不在結果中。',
    },
    '標題': {
      type: '原標題',
      description: '字幕組釋出時的標題，不做改寫。',
    },
    '下載': {
      type: '.torrent 檔案',
      description: '下載連結指向 .torrent 檔案，不提供磁力連結。',
    },
    '分類': {
      type: '5070 / 2000',
      description: '所有番劇資源屬於 5070（Anime）和 5000（TV）。劇場版同時屬於 2000（Movies）和 2020（Movies/Other）。',
    },
    '做種數': {
      type: '可能不顯示',
      description: '只在 AniBT Tracker 的統計足夠新時顯示。沒有資料時不顯示，不會顯示為 0。',
    },
  }}
/>

## 搜尋規則

- 關鍵字結尾的 2 到 4 位數字按集數比對，例如 `Frieren 05`。只有 1 位數字時不當作集數。
- 關鍵字結尾的 1900 至 2099 按年份忽略，例如 `Suzume 2022`。
- 集數按字幕組標註的集數比對。分季重新計數的番劇，Sonarr 傳送的絕對集數可能比對不到後續季。
- 部分資源只有中文標題，用英文或羅馬音搜不到。
- 暫不支援按 TVDB、TMDB 或 IMDb ID 搜尋，這類請求回傳空結果。
- 帶關鍵字搜尋劇場版時，只在符合的前 100 條資源中篩選。同名劇集較多時，劇場版可能不出現在結果裡。

## 限制

<TypeTable
  type={{
    '速率': {
      type: '60 次 / 60 秒 / IP',
      description: '超出後回傳 429 和 Retry-After，用戶端會按該時間暫停。',
    },
    'limit': {
      type: '預設 100，上限 100',
      description: '超過上限按 100 處理。',
    },
    'offset': {
      type: '最多到第 300 條',
      description: '不帶關鍵字時 offset + limit 最多 300；帶關鍵字時最多 100。超出回傳空列表。',
    },
    '結果更新': {
      type: '約 2.5 分鐘',
      description: '相同請求的結果最多延遲約 2.5 分鐘。',
    },
    '服務暫不可用': {
      type: '503',
      description: '帶 Retry-After，用戶端會稍後重試。',
    },
  }}
/>

## 常見問題

<Accordions>
  <Accordion title="Sonarr 測試失敗：no results in configured categories">
    **Anime Categories** 沒有選擇 `5070`。選擇後重新測試。
  </Accordion>
  <Accordion title="回傳 error code 203">
    不支援音樂和圖書搜尋。
  </Accordion>
  <Accordion title="回傳 error code 201">
    參數格式錯誤。例如 `cat` 不是用逗號分隔的數字，或關鍵字超過 160 個 UTF-8 位元組。
  </Accordion>
</Accordions>
