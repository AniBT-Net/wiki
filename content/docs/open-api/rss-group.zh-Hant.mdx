---
title: "字幕組短鏈 RSS"
description: "GET /rss/group/<slug>.xml —— 等價 /rss/anime.xml?groupSlug=<slug>。"
full: true
_openapi:
  preload:
    - ./openapi/anibt.yaml
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          等价 `/rss/anime.xml?groupSlug=<slug>`。不支持 Tag 过滤。

          路径必须以 `.xml` 结尾。未知 slug 返回 `404`。

          规范且无 `limit` 的已收录 group slug 使用 fixed RSS 发布版本；成功响应由 CDN 共享 60 秒并使用
          `must-revalidate`。

          带 `limit` 的 URL 仍是动态 RSS。

          `limit` 默认 100，上限 100。速率限制默认 120 次 / 60 秒 / IP。
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./openapi/anibt.yaml" operations={[{"path":"/rss/group/{slug}.xml","method":"get"}]} />
    </>
  );
}