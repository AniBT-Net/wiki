---
title: LLM 与 Agent 对接
description: Wiki 的纯文本入口、llms.txt 与 OpenAPI 契约，给模型和 Agent 直接读。
---

Wiki 提供纯文本入口。模型和 Agent 可以直接拉 Markdown，不必解析 HTML。

<Callout type="info">
  文档以 **CC-BY-SA 4.0** 发布。引用或再分发时保留原始页面链接。
</Callout>

## 文本入口

<TypeTable
  type={{
    '/llms.txt': {
      type: 'GET',
      description: '站点摘要、目录和核心文档索引（llms.txt 规范）。',
    },
    '/llms-full.txt': {
      type: 'GET',
      description: '全站简体文档拼接成一份纯文本。',
    },
    '/llms-full.en.txt': {
      type: 'GET',
      description: '全站英文文档拼接成一份纯文本。',
    },
    '/openapi.yaml': {
      type: 'GET',
      description: 'AniBT 公开 API 的 OpenAPI 3.1 契约。',
    },
    '/docs/<slug>.md': {
      type: 'GET',
      description:
        '单篇文档的原始 Markdown。例：/docs/apply.md。英文加 /en 前缀，繁体加 /zh-Hant。',
    },
  }}
/>

```bash title="取单篇文档 Markdown"
curl 'https://wiki.anibt.net/docs/apply.md'
```

## 主站页面的 Markdown

AniBT 主站公开 HTML 页（番剧页、发布详情）支持内容协商。请求头带 `Accept: text/markdown` 会返回清洗后的 Markdown。

```bash title="请求番剧页 Markdown"
curl 'https://anibt.net/anime/638151' \
  -H 'Accept: text/markdown'
```

JSON 接口、RSS 和静态文件仍按各自格式返回。
