---
title: LLM & Agent Integration
description: Plain-text Wiki endpoints, llms.txt, and the OpenAPI contract for models and agents.
---

The Wiki exposes plain-text endpoints. Models and agents can fetch Markdown directly instead of parsing HTML.

<Callout type="info">
  Docs are licensed **CC-BY-SA 4.0**. Keep the original page URL when you quote or redistribute.
</Callout>

## Text endpoints

<TypeTable
  type={{
    '/llms.txt': {
      type: 'GET',
      description: 'Site summary, outline, and core doc index (llms.txt).',
    },
    '/llms-full.txt': {
      type: 'GET',
      description: 'All Simplified Chinese docs concatenated as one plain-text file.',
    },
    '/llms-full.en.txt': {
      type: 'GET',
      description: 'All English docs concatenated as one plain-text file.',
    },
    '/openapi.yaml': {
      type: 'GET',
      description: 'OpenAPI 3.1 contract for the AniBT public API.',
    },
    '/docs/<slug>.md': {
      type: 'GET',
      description:
        'Raw Markdown for one doc page. Example: /docs/apply.md. Prefix /en or /zh-Hant for other locales.',
    },
  }}
/>

```bash title="Fetch one doc as Markdown"
curl 'https://wiki.anibt.net/docs/apply.md'
```

## Markdown on the main site

Public HTML pages on AniBT (anime pages, release details) support content negotiation. Send `Accept: text/markdown` to get cleaned Markdown.

```bash title="Request an anime page as Markdown"
curl 'https://anibt.net/anime/638151' \
  -H 'Accept: text/markdown'
```

JSON APIs, RSS feeds, and static files keep their own formats.
