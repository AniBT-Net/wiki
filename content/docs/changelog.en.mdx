---
title: Changelog
description: Release notes for AniBT Wiki and documented AniBT features.
---

This page records AniBT Wiki changes and feature updates that are directly covered by the documentation.

<Callout type="info">
  Dates use Beijing time (UTC+8). Newer updates appear first.
</Callout>

## July 24, 2026

<TypeTable
  type={{
    'Immediate entity-RSS cache invalidation': {
      description: 'After publish events invalidate the matching Redis family, public anime, group, and other-category RSS now purge every Cloudflare CDN query variant for the same bgmId, groupSlug, or category in one operation. URLs, TTLs, ETags, and 304 semantics are unchanged; magnets search and search Redis identities keep their existing TTL behavior.',
      type: 'Improved',
    },
  }}
/>

## July 22, 2026

<TypeTable
  type={{
    'Anime release deletion API': {
      description: 'Added DELETE /api/releases/{releaseId} and GET /api/releases/{releaseId}/deletion. V1 accepts rel_* only; DELETE rejects query parameters and request bodies, returns a stable rdel_* receipt, and uses 200/202 for completed/pending. completed is emitted only after absence is proven across canonical/detail/torrent reads, Typesense search and lists, magnets, public/subscription RSS, Redis, Cloudflare, and AniBT-owned R2 objects. External posts, tracker state, and client-cached/downloaded content are explicit exclusions.',
      type: 'New',
    },
    'Subtitle-group API-key scopes': {
      description: 'API keys now explicitly select at least one of releases:publish and releases:delete; existing keys were safely migrated to releases:publish only. Anime/other publishing requires releases:publish, while deletion and status reads require releases:delete. /api/subtitle-groups/me and publish whoami now expose apiKeyId, apiKeySuffix, and scopes.',
      type: 'Security',
    },
    'Registration and session restoration': {
      description: 'Email registration now runs as send code → atomically verify and create a session → attach the password inside that session. Invalid password format does not consume the code; lost verify/password responses are retryable, and an unexpired flow survives a full-page reload. Public pages show sign-in only after the server explicitly confirms no session, so temporary network/5xx failures no longer create false logout.',
      type: 'Improved',
    },
    'Auth security and OAuth reliability': {
      description: 'Better Auth is now 1.6.23. CAPTCHA tickets use exact path matching and atomic one-time consumption while registration/sign-in bypasses remain closed. KUNGal token exchange uses standard form encoding, and OTP, verification, reset, and welcome mail are durably queued instead of waiting on an external mail provider in the auth request.',
      type: 'Security',
    },
  }}
/>

## July 21, 2026

<TypeTable
  type={{
    'Image batch upload, latency, and progress': {
      description: 'Upload preparation handles up to five images in one membership check and one control-plane transaction, while one batch GET and one Convex query now poll every active image together. The R2 PUT ETag travels with the worker job, and one If-Match conditional read replaces staging HEAD requests before and after the body read. Supported browsers pre-scale oversized static JPEG/PNG/WebP files only when the result is meaningfully smaller; animation, HEIC/HEIF, and other skipped formats still go through the worker. The per-image limit remains 10 MiB.',
      type: 'Optimized',
    },
  }}
/>

## July 20, 2026

<TypeTable
  type={{
    'Direct image upload and isolated transcoding': {
      description: 'Subtitle-group image hosting now uploads browser bytes directly to staging R2. The main Web runtime only handles authorization, presigning, object verification, and status queries; it no longer reads image bytes or transcodes them. The shared Bun worker starts a short-lived child per image and uses native Bun.Image to write directly to Bun.S3File. Damaged or failed static-image transcodes fail explicitly, while animation and SVG remain preserved.',
      type: 'Optimized',
    },
  }}
/>

## July 19, 2026

<TypeTable
  type={{
    'Fixed tracker URL restored': {
      description: 'AniBT has restored https://tracker.anibt.net/announce with no signed dual path. Existing AniBT torrents need no URL replacement or data re-download. Magnets, RSS, and canonical .torrent downloads now contain exactly one fixed AniBT tracker while preserving the infohash.',
      type: 'Fixed',
    },
    'Tracker whitelist and edge protection': {
      description: 'The fixed /announce endpoint still serves only v1 info_hash values admitted by AniBT; external hashes receive not whitelisted. Cloudflare retains 20-byte info_hash/peer_id validation and a basic burst limit. Public /scrape, /api/*, /announce/v1/*, and UDP remain closed.',
      type: 'Security',
    },
  }}
/>

## July 18, 2026

<TypeTable
  type={{
    'Site-wide Markdown content negotiation': {
      description: 'AniBT public HTML pages now support Accept: text/markdown through real-time Cloudflare edge conversion. Vary: accept keeps HTML and Markdown cache variants separate, token estimates are exposed, and JSON APIs, RSS/XML, images, and R2 objects keep their native formats. The current public Content-Signal is ai-train=yes, search=yes, ai-input=yes.',
      type: 'Added',
    },
    'Strict public RSS request contract': {
      description: 'Every public RSS endpoint now accepts exactly one limit from 1..100 (default 100); oversized, repeated, empty, zero-padded, or non-integer values are no longer silently truncated. q/search allow one value total with NFKC, trim, whitespace collapse, and a 160 UTF-8-byte cap. Canonical URLs return a one-day-cacheable 308; invalid input returns application 400 and may be blocked first by production Cloudflare 403.',
      type: 'Changed',
    },
    'High-cardinality search MISS protection': {
      description: 'Magnets/other searches and empty results cache for 600 seconds while latest/filter variants cache for 120 seconds. Stable queries and cache HITs do not consume new-query budget. New high-cardinality MISSes use 10-minute/24-hour distinct-query budgets and a global concurrency lease; rejection returns 429 + Retry-After. Redis keys and structured logs retain only query hashes, never raw search text.',
      type: 'Improved',
    },
    'Indexer edge and origin hardening': {
      description: 'Normal Jackett, Auto_Bangumi, and ani-rss requests remain compatible. Cloudflare blocks invalid limits at edge, dynamic origin protection remains 500 requests per 10 seconds, and publish invalidation now uses exact URLs instead of clearing every search variant. Front proxies add edge-bypass validation, privacy-safe sampled logs, and reliable rotation.',
      type: 'Operations',
    },
    'Latin-diacritic title equivalence': {
      description: 'Pure-Latin titles keep both the original diacritic token and a folded equivalent, such as Caraméliser / Carameliser; CJK and mixed-script text are not folded. If folding produces candidates from multiple anime, the matcher keeps all of them and fails closed instead of auto-binding the first result.',
      type: 'Fixed',
    },
    'Web manual-upload evidence loop': {
      description: 'Web torrent upload now verifies group membership, parses the real info.name, and replays v3 server-side; callers cannot forge the project fingerprint. A matcher result that already selects the same anime does not train itself. Manual or different-target confirmation starts pending, a second distinct episode activates, repeated episodes do not increment, and conflicts suspend. Matcher outages, internal exceptions, and oversized info.name values do not block an already validated explicit selection and do not learn a binding.',
      type: 'Fixed',
    },
    'Matcher generation 2040': {
      description: 'Production now uses match_v3_20260718_2040 with 8,621 anime candidates, 223,792 aliases, and 168 scoped bindings. The fixed 34,083-item corpus has 28,559 automatic decisions (83.7925%); the 2024–2026 subset is 9,084 / 9,807 (92.6277%). Both Carameliser and Caraméliser now auto-match BGM 617123 in production.',
      type: 'Data',
    },
  }}
/>

## July 17, 2026

<TypeTable
  type={{
    'Publish API group whoami': {
      description: '`GET /api/releases/publish` and `GET /api/other-releases/publish` return the API key’s bound groupId/groupName/groupSlug/apiKeySuffix without writing. Formal publish and preview success responses also echo the same identity fields so automation can detect a wrong-group key before durable write.',
      type: 'Added',
    },
    'Block empty magnet-only formal publishes': {
      description: 'When there is no torrent and fileSize cannot be resolved to >0 (including magnets without xl=), formal publish returns 422 / reason=EMPTY_RELEASE_CONTENT. Preview still allows 0 B so operators can check match status and group identity.',
      type: 'Fixed',
    },
    'Manual match project fan-out': {
      description: 'When a fansub member or admin manually matches a pending release, “remember project” and “also match other pending releases in this project” default on. One confirmation activates the group+projectFingerprint scoped binding and retroactively matches up to 64 pending siblings with the same fingerprint. Never crosses groups.',
      type: 'Improved',
    },
    'Official abbreviation alias proposal queue': {
      description: 'Admins get /admin/anime-alias-proposals. An offline miner clusters latin abbreviation gaps from low_evidence failures; proposals must be approved before writing a manual alias and triggering projection sync. Rejected (animeId, normalizedAlias) pairs stay dead. Runtime thresholds are unchanged and nothing auto-writes aliases.',
      type: 'Added',
    },
  }}
/>

## July 16, 2026

<TypeTable
  type={{
    'RSS emits anibt:bgmId': {
      description: '`/rss/magnets.xml`, `/rss/anime.xml`, and `/rss/group/<slug>.xml` anime items now include `<anibt:bgmId>` (Bangumi subject id when bound) under xmlns:anibt. `/rss/other/*` does not. Standard readers can ignore the namespace.',
      type: 'Added',
    },
    'Torrent matcher v3 cutover': {
      description: 'The canonical API is hard-cut to POST /api/animes/torrent-match. GET returns JSON 405, with no v2 DTO, cache, or compatibility layer. The server parses release title and torrent internal name independently, builds a multilingual AST, and separates exact, strict-recall, and fuzzy-recall retrieval. Any overflow, cutoff, missing hydration, or generation/projection mismatch blocks automatic binding.',
      type: 'Improved',
    },
    'Episode-boundary and version-year closure': {
      description: 'Bare numeric brackets are now contextual: 24 is an episode in [Title][24][1080p] and remains frame-rate metadata in [Title][1080p][24][AAC]. A 2024 edition / (2024) marker must match the candidate premiere year exactly. A one-episode special-count offset is repaired for a boundary episode or exact whole-season range only when family completion, later-season ranking, publication time, and a unique target all agree.',
      type: 'Improved',
    },
    'Safe non-anime and bundle exits': {
      description: 'Standalone PV/CM, MAD/AMV, game videos, music packages, announcements, wallpapers, and font packages may return non_anime. Multi-anime, multi-season, and S1+S2+OVA bundles return unsupported instead of being forced onto one anime.',
      type: 'Added',
    },
    'One matcher across publishing': {
      description: 'Publish UI, bulk publishing, manual matching, and API-key publishing now share the server-side v3 matcher. When no anime ID is supplied, the publish API runs the same decision directly; caller-forged internal match assertions are discarded, and only decision.kind=auto binds immediately.',
      type: 'Improved',
    },
    'Clean production match index': {
      description: 'Production now uses match_v3_20260716_2112 with 8,621 anime candidates, 223,778 aliases, and 24 scoped bindings. On the fixed 34,083-item corpus, 9,091 / 9,781 anime releases from 2024–2026 are automatic (92.9455%); excluding VCB bundles planned for separate handling, the result is 8,744 / 9,339 (93.6289%). TMDB/TVDB do not participate in matching.',
      type: 'Data',
    },
  }}
/>

## July 14, 2026

<TypeTable
  type={{
    'Torrent-title anime matching': {
      description: '/api/animes/torrent-match now uses the dedicated anime_match_aliases index to globally disambiguate Chinese, Traditional Chinese, Japanese, romaji, English, multilingual title lists, and Season/Cour/Part/Sequence/Format evidence. The old allTitles text-score margin no longer authorizes binding. Long multilingual exact queries now use a request body instead of the Typesense GET query string, avoiding the 4,000-character limit.',
      type: 'Improved',
    },
    'Explainable auto-binding contract': {
      description: 'Responses add decisionReason, exactAnimeCount, vetoCategory, and bounded evidenceSummary. Clients may automatically use bestMatch only when shouldAutoSelect=true; shared titles, modifier conflicts, weak evidence, and exact overflow remain manual.',
      type: 'Added',
    },
    'Safer matcher caching': {
      description: 'The retired GET matcher once used a 60-second shared cache with stale fallback disabled. The July 16 POST v3 hard cut removed that GET route, v2 DTO, and cache; current matcher responses are always no-store.',
      type: 'Improved',
    },
  }}
/>

## July 12, 2026

<TypeTable
  type={{
    'Publish and upload error contract': {
      description: 'JSON magnet and multipart torrent failures on the anime and other-release publish APIs now share an ok=false error envelope with machine code, a safe message, optional reason, retryable, retryAfterMs, requestId, and i18n metadata. X-Request-ID can be used to correlate server logs.',
      type: 'Improved',
    },
    'Actionable publish status codes': {
      description: 'A planned deployment handoff pause now returns 503 Service Unavailable with Retry-After and reason=DEPLOYMENT_IN_PROGRESS. Duplicate release versions retain 409 with reason=RELEASE_VERSION_EXISTS. Write throttling returns 429 with reason=RATE_LIMITED and an explicit backoff delay.',
      type: 'Improved',
    },
  }}
/>

## July 11, 2026

<TypeTable
  type={{
    'Publish success and public visibility': {
      description: 'The documentation now clarifies that a publish API 200 OK means the AniBT write was accepted and downstream work was handed off reliably; it is not a synchronous guarantee that the release is already present in RSS, search, public pages, every CDN location, or an external site. Callers should verify the returned releaseId in the target body; HTTP 200, generatedAt, ETag, or a cache hit alone is not proof of updated content.',
      type: 'Docs',
    },
    'Ordered public snapshot publication': {
      description: 'Public magnets, RSS, anime/group/release-detail snapshots, and related pages now use immutable R2 generations plus PostgreSQL publication heads. Web reads and strictly warms only the generation selected by the head; Redis publication-order / generation fences reject stale writeback, and page-ready is emitted only after every required variant completes. Anime updates and deletes continue to handle old/new BGM aliases and affected sitemaps.',
      type: 'Improved',
    },
    'Reliable Edge invalidation': {
      description: 'CDN invalidation now uses a durable URL queue and frozen runs/batches. Token + generation-fenced leases prevent stale executors from acknowledging work; failures resume, corrupt state is quarantined rather than silently discarded, and bounded retention keeps processed markers for 14 days, completed runs for 14 days, and quarantined runs for 30 days.',
      type: 'Improved',
    },
    'Freshness operations gate': {
      description: 'Added metrics, alerts, and preflight checks for snapshot queue/claim delay, publication pending/latency, head/watermark integrity, generation maintenance/GC, and Edge D1 durable flush. High queue age with low Redis warm latency is treated as scheduling/backlog; extending TTLs is not an accepted workaround for propagation gaps.',
      type: 'Operations',
    },
  }}
/>

## July 7, 2026

<TypeTable
  type={{
    'Unified anime IDs for publishing': {
      description: 'The anime release publish API now supports animeIdType + animeId, where animeIdType can be bgm, anilist, mal, or anidb. JSON clients can also send anime: { source, id }. The historical bgmId field remains compatible.',
      type: 'Added',
    },
    'Group API examples': {
      description: 'The group API publish page, Nyaa proxy guide, and Open API docs now use unified anime ID examples and include a preview=true smoke test; external ID mappings follow bangumi-data, and missing or conflicting mappings become pending releases.',
      type: 'Docs',
    },
  }}
/>

## July 6, 2026

<TypeTable
  type={{
    'Release language codes': {
      description: 'The anime release publish API, preview publish flow, group publish defaults, and RSS language filters now support CHS, CHT, JP, EN, KO, ES, PT, FR, DE, IT, RU, AR, HI, ID, MS, TH, VI, TL, TR, PL, and UK. The language array no longer has a 4-item limit, so multi-language subtitle packs can list every supported language.',
      type: 'Changed',
    },
    'Anime admin metadata cleanup': {
      description: 'Anime banner and episode metadata sync has been stopped. Anime detail snapshots, Typesense anime documents, and public episode metadata queries no longer expose those metadata rows. The admin anime page removes banner / episode edit and sync controls, uses a stable row-based list, and adds a maintenance path for batching cleanup of old animeEpisodes, animeSourceEpisodes, banner fields, and legacy animeSyncMeta fields.',
      type: 'Changed',
    },
    'Seasonal anime title query': {
      description: '/api/seasons/anime now accepts query (short alias: q) to search by Chinese, Traditional Chinese, romaji, English, and Japanese anime titles. Without season it searches all public anime; with season it is scoped to that season. It cannot be combined with bgmId.',
      type: 'Added',
    },
  }}
/>

## July 5, 2026

<TypeTable
  type={{
    'Anime detail and subscription preview': {
      description: 'Anime detail pages now use a two-column layout, and release / episode lists share a consistent release row. The subscribe dialog previews matches from the show’s recent releases as you adjust groups, resolution, language, subtitle type, file format, or custom tags; after saving, the personal subscription RSS still exports the final server-side subscription results.',
      type: 'Added',
    },
    'Subtitle group image host': {
      description: 'Added /groups/<slug>/image-host. The current interface uses a single upload entry followed by image management, with no top mode switcher or two-column upload panel. It supports choosing, dropping, and pasting image uploads, then shows BBCode, HTML, Markdown, and direct-link formats directly in the page result area.',
      type: 'Added',
    },
    'Image host copy and moderation': {
      description: 'Image management now uses a dense small-thumbnail grid with lazy image loading and paginated loading, while the full copy-format set still opens in a dialog. The site-admin /admin/image-host review page uses the same dense thumbnail approach for scanning recent uploads across subtitle groups and deleting sensitive or policy-breaking images.',
      type: 'Added',
    },
    'Image host permissions and deployment': {
      description: 'Image hosting is limited to subtitle group members and checks membership before reading the body. Delete permissions are limited to the uploader, group owner/admin, or site admin, and deletion now soft-deletes Convex metadata before a media worker asynchronously deletes the object. The Bun native image pipeline converts static images to WebP when possible, preserves animated GIF/WebP/APNG, SVG, and reliably unconvertible images in their original format. Docs now cover the dedicated object bucket, Bun.Image/Bun.S3Client runtime gate, and required Web/worker environment variables.',
      type: 'Docs',
    },
  }}
/>

## July 4, 2026

<TypeTable
  type={{
    'Group sync workbench': {
      description: '/groups/<slug>/sync now uses a per-site workbench: target selector on the left, credential configuration in the middle, recent logs on the right, plus a link to claim management.',
      type: 'Changed',
    },
    'Sync log retention': {
      description: 'Sync logs show the latest 40 entries, support status filtering, and are automatically cleaned after about 14 days. Deleted-release logs keep the release ID but disable retry and resync actions.',
      type: 'Changed',
    },
    'Public search page guard': {
      description: '/magnets page numbers and /api/animes/search cursors now paginate at most 100 Typesense pages. Invalid search cursors return 400, while oversized page numbers normalize to page 100 to avoid slow deep pagination.',
      type: 'Changed',
    },
    'Seasonal release availability': {
      description: '/api/seasons/anime anime items now include rssReleaseCount and hasRelease so clients can tell whether a show already has AniBT releases. The home weekly cards use the same state to show "No torrents yet".',
      type: 'Added',
    },
    'RSS browser rendering': {
      description: 'All RSS endpoints now render as native XML when opened in a browser. AniBT no longer attaches custom XSL/CSS; RSS readers and BT clients continue using the standard RSS fields.',
      type: 'Changed',
    },
  }}
/>

## June 27, 2026

<TypeTable
  type={{
    'Other Release Nyaa Proxy API': {
      description: 'The other release publish API now supports the same nyaa, nyaaCategory, nyaaComplete, nyaaRemake, nyaaDescription, and nyaaInformation parameters as anime publishing.',
      type: 'Added',
    },
    'API Key Guidance': {
      description: 'Updated the API key page and Open API docs with other release Nyaa proxy fields, tracker requirements, multipart examples, and common category suggestions.',
      type: 'Docs',
    },
  }}
/>

## June 13, 2026

<TypeTable
  type={{
    'Publish API Parameters': {
      description: 'Expanded anime release publish API docs for language, subtitle, resolution, format, torrent, magnet, tracker, preview, and Nyaa proxy fields.',
      type: 'Docs',
    },
    'Nyaa Category Mapping': {
      description: 'Added the full Nyaa category code table, clarified that Chinese anime subtitles usually use 1_3, and documented complete, remake, description, and information proxy parameters.',
      type: 'Docs',
    },
    'Other Release Publish API': {
      description: 'Documented manga, music, raw, and stage category codes, RSS mapping, and field usage for customTags, magnetLink, and magnetBase64.',
      type: 'Docs',
    },
  }}
/>

## May 26, 2026

<TypeTable
  type={{
    'Bangumi Moe Sync': {
      description: 'Added configuration and release sync support for Bangumi Moe (bangumi.moe).',
      type: 'Added',
    },
    'ACGNX Sync': {
      description: 'Added UID and API Token configuration for ACGNX sync, so AniBT releases can be mirrored there.',
      type: 'Added',
    },
    'AniBT Wiki': {
      description: 'Launched this Wiki with documentation for subtitle group admission, site sync, Tracker usage, Open API, and LLM integration.',
      type: 'Launched',
    },
  }}
/>
